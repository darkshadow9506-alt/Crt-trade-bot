# CRT Trade Bot 🐢📈

A multi-timeframe **Candlestick Range Theory (CRT)** day-trading engine in
Python — Smart-Money-Concepts analysis, a look-ahead-free backtester, prop-firm
risk guardrails, and Telegram signals.

> ربات تریدی که دقیقاً استراتژی CRT خودت رو پیاده می‌کنه: تحلیل چند تایم‌فریمی،
> بک‌تست بدون خطای آینده‌نگری، مدیریت ریسک به سبک پراپ‌فرم، و ارسال سیگنال به تلگرام.

---

## استراتژی (دقیقاً طبق چیزی که تعریف کردی)

ساختار **HTF → MTF → LTF** بعد از باز شدن سشن نیویورک:

1. **HTF (context):** بعد از باز شدن سشن، صبر می‌کنیم تا قیمت به یه نقطه مهم برسه
   (**FVG / OB / سوئیپ نقدینگی سشن‌های قبل**) و یه کندل **CRT** بزنه؛ یعنی شدوش
   نقدینگی یه طرف رنج رو بزنه و دوباره داخل رنج ببنده. این جهت و رنج معامله رو
   مشخص می‌کنه.
2. **MTF:** منتظر یه **CHoCH** هم‌جهت CRT می‌مونیم و بعد یه **پولبک به ناحیه
   فیبوناچی ۰٫۶۱۸ تا ۰٫۷۸۶**.
3. **LTF:** دوباره یه **CHoCH**، بعد ورود روی **iFVG** یا **CISD**.

**ریسک:** حد ضرر پشت سقف/کف کندل پولبک (+ کمی پدینگ ATR).

**تارگت (دقیقاً طبق قانون تو):**
- بازار **صعودی** + سیگنال **لانگ** → TP = **CRT High**
- بازار **نزولی** + سیگنال **لانگ** → TP = **۵۰٪ فیبوناچی** بین CRT H و CRT L (equilibrium).
  اگه قیمت **قبل از ورود** به ۵۰٪ برخورد کرده بود → **کل ستاپ کنسل**.
- **شورت** دقیقاً برعکس (بازار نزولی+شورت → CRT Low ؛ بازار صعودی+شورت → ۵۰٪).

**سخت‌گیری:** سیگنال فقط وقتی صادر می‌شه که **همه** معیارها با هم باشن
(POI + CRT + CHoCH میدل + پولبک ۰٫۶۱۸–۰٫۷۸۶ + CHoCH پایین + iFVG/CISD + RR کافی).
اگه حتی یکی نباشه، **هیچ سیگنالی داده نمی‌شه**.

ست‌های تایم‌فریم:

| تنظیم | HTF | MTF | LTF |
|------|-----|-----|-----|
| `1D-1H-15min`   | 1D  | 1H    | 15min |
| `4H-15min-5min` | 4H  | 15min | 5min  |
| `1H-5min-1min`  | 1H  | 5min  | 1min  |

---

## نصب

```bash
pip install -r requirements.txt        # pandas, numpy, pyyaml, requests, pytest
cp config/config.example.yaml config/config.yaml
```

## بک‌تست (شروع از اینجا)

```bash
# دیتای نمونه ساختگی بساز (یا CSV خودت رو بذار)
python scripts/generate_sample_data.py

# اجرا روی ست تایم‌فریم انتخابی در config.yaml
python -m crt_bot.cli backtest --save

# مقایسه‌ی هر سه ست تایم‌فریم روی یک دیتا
python examples/run_backtest.py
```

فرمت CSV ورودی (نام ستون‌ها انعطاف‌پذیره): `time, open, high, low, close[, volume]`.

نمونه خروجی گزارش:

```
========================================================
  CRT Backtest  |  BTCUSD  |  TF set 1H-5min-1min
========================================================
  Trades            : 12  (W 7 / L 5)
  Win rate          : 58.3%
  Profit factor     : 2.10
  Avg R:R (planned) : 2.45
  ...
  Total return      : +6.40%
  Max drawdown      : 2.10%
========================================================
```

---

## معماری

```
crt_bot/
├── core/         # مدل‌ها، تایم‌فریم/resample، سشن، اندیکاتورها (ATR)
├── smc/          # تشخیص‌دهنده‌ها: structure(CHoCH/BOS), fvg/ifvg, order_block,
│                 #   liquidity sweep, crt, fib, cisd
├── strategy/     # ماشین حالت CRT (HTF→MTF→LTF) ← قلب سیستم
├── risk/         # سایزینگ + قوانین پراپ (ضرر روزانه/دراودان/تارگت)
├── backtest/     # موتور event-driven بدون look-ahead + گزارش‌گیری
├── data/         # لودر CSV و resample
├── feeds/        # فید دیتای زنده: binance / toobit / mt5 / csv-replay
├── live/         # حلقه زنده: فید → استراتژی → سیگنال تلگرام + ردیابی مجازی
├── notify/       # کارت سیگنال + ارسال تلگرام
└── cli.py        # رابط خط فرمان (backtest / live)
```

**بدون خطای آینده‌نگری:** موتور روی کوچک‌ترین تایم‌فریم حرکت می‌کنه و در هر کندلِ
بسته‌شده فقط کندل‌هایی از هر تایم‌فریم رو به استراتژی می‌ده که تا اون لحظه واقعاً
بسته شدن. معاملات هم روی کندل‌های *بعدی* مدیریت می‌شن (پر شدن SL/TP).

---

## مدیریت ریسک / پراپ‌فرم

در `config.yaml` بخش `risk`:

- `risk_per_trade_pct` — درصد ریسک هر معامله (سایز پوزیشن خودکار حساب می‌شه)
- `max_daily_loss_pct` — سقف ضرر روزانه (هر روز ریست می‌شه؛ مثل FTMO)
- `max_total_drawdown_pct` — حداکثر دراودان کل (ربات متوقف می‌شه)
- `profit_target_pct` — تارگت سود فاز

---

## سیگنال زنده + ارسال خودکار به تلگرام

ربات بازار رو مدام چک می‌کنه و وقتی ستاپ CRT **کامل** شد، یه کارت سیگنال
**کامل** (جهت، نماد، تایم‌فریم‌ها، Entry / SL / TP / R:R، رنج CRT، نقطه ۵۰٪،
بایاس بازار، قانون TP، تریگر، و سایز پیشنهادی) رو **خودکار** به تلگرام می‌فرسته.
بعدش معامله رو به‌صورت مجازی دنبال می‌کنه و موقع برخورد TP/SL هم نتیجه
(مثلاً `+3.68R` یا `-1.00R`) رو پیام می‌ده.

نمونه کارت سیگنال:

```
🟢 LONG  BTCUSDT
⏱ 1H ▸ 5min ▸ 1min  ·  new_york session
🕐 2026-05-02 14:37 UTC

🎯 Entry : 62,553.83
🛑 SL    : 62,272.68
✅ TP    : 63,587.51
📊 R:R   : 3.68
💰 Risk 0.5% (≈ 500.00)  →  size ≈ 1.7784

📦 CRT range : 62,341.11 – 63,587.51
⚖️ 50% (eq)  : 62,964.31
📈 Market bias: BULLISH
🎯 TP rule   : CRT High (trend-aligned)
🔑 Trigger   : IFVG after LTF CHoCH
✓ POI + CRT + MTF CHoCH + 0.618–0.786 pullback + LTF CHoCH + entry
```

**۱) تلگرام رو در `config.yaml` تنظیم کن:**

```yaml
notify:
  telegram:
    enabled: true
    bot_token: "123:ABC"   # از @BotFather
    chat_id: "123456789"   # آی‌دی چت یا کانال
```

**۲) منبع دیتای زنده رو انتخاب کن** (بخش `live` در `config.yaml`):

```yaml
live:
  feed: "binance"          # binance | toobit | mt5 | csv
  symbol: "BTCUSDT"        # نماد دقیق فید (بایننس/توبیت: BTCUSDT ؛ MT5: XAUUSD ...)
  poll_seconds: 30
```

| فید | کاربرد | پیش‌نیاز |
|-----|--------|----------|
| `binance` | کریپتو، بدون کلید (بهترین دیتا) | فقط `requests` |
| `toobit`  | کریپتو، بدون کلید — **جایگزین وقتی بایننس تحریم/مسدوده** | فقط `requests` |
| `mt5`     | فارکس/طلا/ایندکس (پراپ) | `pip install MetaTrader5` + ترمینال MT5 (ویندوز/VPS) |
| `csv`     | replay آفلاین برای تست | فقط یه فایل CSV |

> 🌍 **تحریم/دسترسی:** بایننس بعضی کشورها (مثل ایران) رو مسدود می‌کنه. اگه برات
> مشکل‌سازه از `toobit` استفاده کن (API سازگار با بایننس، بدون کلید). اگه دسترسی
> به بایننس داری، دیتای بایننس بهتره.

**۳) اجرا:**

```bash
python -m crt_bot.cli live --test-telegram     # یه پیام تستی بفرست و خارج شو
python -m crt_bot.cli live                      # حلقه زنده‌ی واقعی
python -m crt_bot.cli live --steps 200          # محدود (تست/replay)
```

هر فید فقط کندل‌های **بسته‌شده** رو می‌ده (کندل در حال شکل‌گیری حذف می‌شه) تا
سیگنال‌ها معتبر باشن. سیگنال‌های تکراری ارسال نمی‌شن و وضعیت در
`reports/live_state.json` ذخیره می‌شه تا بعد از ری‌استارت دوباره فرستاده نشه.

---

## مسیر پیشِ‌رو (Roadmap)

- [x] هسته SMC + استراتژی CRT چند تایم‌فریمی
- [x] موتور بک‌تست بدون look-ahead + گزارش پراپ‌فرم
- [x] تست‌های واحد برای تشخیص‌دهنده‌ها و استراتژی
- [x] **سیگنال زنده + ارسال خودکار به تلگرام** (فید Binance / MT5 / CSV-replay)
- [x] ردیابی مجازی معامله و پیام نتیجه TP/SL
- [ ] **اجرای واقعی سفارش:** آداپتور MT5 برای پراپ‌فرم و ccxt برای کریپتو
- [ ] بهینه‌سازی پارامترها (walk-forward) و آمار تفکیکی به ازای سشن/جهت

> فید دیتا و ارسال سیگنال پلاگین‌این هستن: همون `Signal` که تولید می‌شه، بعداً
> برای **اجرای واقعی** مستقیم به MT5/صرافی پاس داده می‌شه (فعلاً فقط سیگنال).

---

## تست

```bash
python -m pytest -q
```

## ⚠️ هشدار

این نرم‌افزار آموزشی/تحقیقاتیه. نتایج بک‌تست تضمین سود آینده نیست. قبل از هر
استفاده‌ی واقعی، روی حساب دمو و دیتای واقعی اعتبارسنجی کن و قوانین پراپ‌فرم خودت
(از جمله مجاز بودن اتومیشن) رو چک کن.
